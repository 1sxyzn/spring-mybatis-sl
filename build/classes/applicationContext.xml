<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xmlns:p="http://www.springframework.org/schema/p"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- 컴포넌트 스캔 설정 / 컨테이너야 이 패키지로 시작하는 클래스들 중에 컴포넌트 어노테이션이 붙은 것을 찾아라
		클래스 이름이 바뀌어도 xml을 변경하지 않아도 (빈 등록을 안해도) 메모리에 뜬다.
		컴포넌트 스캔을 안하려면 <bean class="polymorphism3.LgTV"> 해야함. 
		
		id 는 따로 지정하지 않으면 클래스이름 복사해서, 첫글자만 소문자로 바꿔서 실행.
		@Component("아이디") 로 하면 아이디로 lookup 가능-->
	<!--
		@Component: 유지보수 과정에서 변경되지 않는 객체
		bean 등록: 유지보수 과정에서 자주 변경되는 객체
	 -->
	<context:component-scan base-package="polymorphism3"/>
	
	<bean class="polymorphism3.SonySpeaker"></bean>
	<!-- 어노테이션 설정과 xml 설정을 병행해서 써야지, 나중에 자바코드를 수정하지 않아도 됨. 모든 것을 어노테이션으로 할 수는 X -->
	
</beans>